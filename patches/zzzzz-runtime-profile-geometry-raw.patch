diff --git a/dom/base/nsScreen.cpp b/dom/base/nsScreen.cpp
index b9bbf780c9..f9673b5a02 100644
--- a/dom/base/nsScreen.cpp
+++ b/dom/base/nsScreen.cpp
@@ -47,6 +47,29 @@ NS_IMPL_CYCLE_COLLECTION_INHERITED(nsScreen, DOMEventTargetHelper,
                                    mScreenOrientation)
 
 int32_t nsScreen::PixelDepth() {
+  if (auto value = MaskConfig::GetInt32("screen.colorDepth")) {
+    return *value;
+  }
+  if (auto value = MaskConfig::GetInt32("screen.pixelDepth")) {
+    return *value;
+  }
+
+  if (nsPIDOMWindowOuter* outer = GetOuter()) {
+    if (nsPIDOMWindowInner* inner = outer->GetCurrentInnerWindow()) {
+      nsGlobalWindowInner* win = nsGlobalWindowInner::Cast(inner);
+      if (win) {
+        uint32_t userContextId =
+            mozilla::dom::ScreenDimensionManager::GetUserContextIdFromWindow(
+                win);
+        int32_t value =
+            mozilla::dom::ScreenDimensionManager::GetColorDepth(userContextId);
+        if (value > 0) {
+          return value;
+        }
+      }
+    }
+  }
+
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getColorDepth()) {
       return *value;
@@ -78,7 +101,24 @@ nsDeviceContext* nsScreen::GetDeviceContext() const {
 }
 
 CSSIntRect nsScreen::GetRect() {
-  // Check for per-context screen dimensions first
+  // Prefer launch-level Rotunda profiles over protocol viewport bookkeeping.
+  // Juggler uses ScreenDimensionManager for Playwright viewport emulation, but
+  // that must not replace the fingerprinted screen seen by page scripts.
+  auto configuredWidth = MaskConfig::GetInt32("screen.width");
+  auto configuredHeight = MaskConfig::GetInt32("screen.height");
+  if (configuredWidth && configuredHeight) {
+    return {0, 0, *configuredWidth, *configuredHeight};
+  }
+
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    auto width = screen->getWidth();
+    auto height = screen->getHeight();
+    if (height && width) {
+      return {0, 0, *width, *height};
+    }
+  }
+
+  // Fall back to per-context screen dimensions when there is no launch profile.
   if (nsPIDOMWindowOuter* outer = GetOuter()) {
     if (nsPIDOMWindowInner* inner = outer->GetCurrentInnerWindow()) {
       nsGlobalWindowInner* win = nsGlobalWindowInner::Cast(inner);
@@ -92,14 +132,6 @@ CSSIntRect nsScreen::GetRect() {
     }
   }
 
-  // Fallback to global MaskConfig override
-  if (auto screen = MaskConfig::Profile().getScreen()) {
-    auto width = screen->getWidth();
-    auto height = screen->getHeight();
-    if (height && width) {
-      return {0, 0, *width, *height};
-    }
-  }
   // Return window inner rect to prevent fingerprinting.
   if (ShouldResistFingerprinting(RFPTarget::ScreenRect)) {
     return GetTopWindowInnerRectForRFP();
@@ -132,7 +164,25 @@ CSSIntRect nsScreen::GetRect() {
 }
 
 CSSIntRect nsScreen::GetAvailRect() {
-  // Check for per-context screen avail dimensions first
+  // Prefer launch-level Rotunda profiles over protocol viewport bookkeeping.
+  auto configuredWidth = MaskConfig::GetInt32("screen.availWidth");
+  auto configuredHeight = MaskConfig::GetInt32("screen.availHeight");
+  if (configuredWidth && configuredHeight) {
+    return {MaskConfig::GetInt32("screen.availLeft").value_or(0),
+            MaskConfig::GetInt32("screen.availTop").value_or(0),
+            *configuredWidth, *configuredHeight};
+  }
+
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    auto width = screen->getAvailWidth();
+    auto height = screen->getAvailHeight();
+    if (width && height) {
+      return {screen->getAvailLeft().value_or(0),
+              screen->getAvailTop().value_or(0), *width, *height};
+    }
+  }
+
+  // Fall back to per-context screen avail dimensions when there is no launch profile.
   if (nsPIDOMWindowOuter* outer = GetOuter()) {
     if (nsPIDOMWindowInner* inner = outer->GetCurrentInnerWindow()) {
       nsGlobalWindowInner* win = nsGlobalWindowInner::Cast(inner);
@@ -153,14 +203,6 @@ CSSIntRect nsScreen::GetAvailRect() {
     }
   }
 
-  if (auto screen = MaskConfig::Profile().getScreen()) {
-    auto width = screen->getAvailWidth();
-    auto height = screen->getAvailHeight();
-    if (width && height) {
-      return {screen->getAvailLeft().value_or(0),
-              screen->getAvailTop().value_or(0), *width, *height};
-    }
-  }
   // Return window inner rect to prevent fingerprinting.
   if (ShouldResistFingerprinting(RFPTarget::ScreenAvailRect)) {
     return GetTopWindowInnerRectForRFP();
diff --git a/dom/base/nsGlobalWindowInner.cpp b/dom/base/nsGlobalWindowInner.cpp
index 672861a977..cab935462a 100644
--- a/dom/base/nsGlobalWindowInner.cpp
+++ b/dom/base/nsGlobalWindowInner.cpp
@@ -3706,6 +3706,9 @@ void nsGlobalWindowInner::SetName(const nsAString& aName,
 }
 
 double nsGlobalWindowInner::GetInnerWidth(ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.innerWidth")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowInnerWidth(userContextId, &value)) {
@@ -3727,6 +3730,9 @@ nsresult nsGlobalWindowInner::GetInnerWidth(double* aWidth) {
 }
 
 double nsGlobalWindowInner::GetInnerHeight(ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.innerHeight")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowInnerHeight(userContextId, &value)) {
@@ -3753,6 +3759,9 @@ nsresult nsGlobalWindowInner::GetInnerHeight(double* aHeight) {
 
 int32_t nsGlobalWindowInner::GetOuterWidth(CallerType aCallerType,
                                            ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.outerWidth")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowOuterWidth(userContextId, &value)) {
@@ -3769,6 +3778,9 @@ int32_t nsGlobalWindowInner::GetOuterWidth(CallerType aCallerType,
 
 int32_t nsGlobalWindowInner::GetOuterHeight(CallerType aCallerType,
                                             ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.outerHeight")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowOuterHeight(userContextId, &value)) {
@@ -3793,6 +3805,9 @@ double nsGlobalWindowInner::ScreenEdgeSlopY() const {
 
 int32_t nsGlobalWindowInner::GetScreenX(CallerType aCallerType,
                                         ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.screenX")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowScreenX(userContextId, &value)) {
@@ -3806,6 +3821,9 @@ int32_t nsGlobalWindowInner::GetScreenX(CallerType aCallerType,
 
 int32_t nsGlobalWindowInner::GetScreenY(CallerType aCallerType,
                                         ErrorResult& aError) {
+  if (auto value = MaskConfig::GetInt32("window.screenY")) {
+    return *value;
+  }
   int32_t value = 0;
   uint32_t userContextId = ScreenDimensionManager::GetUserContextIdFromWindow(this);
   if (ScreenDimensionManager::GetWindowScreenY(userContextId, &value)) {
@@ -3847,6 +3865,9 @@ static nsPresContext* GetPresContextForRatio(Document* aDoc) {
 double nsGlobalWindowInner::GetDevicePixelRatio(CallerType aCallerType,
                                                 ErrorResult& aError) {
   ENSURE_ACTIVE_DOCUMENT(aError, 0.0);
+  if (auto value = MaskConfig::GetDouble("window.devicePixelRatio")) {
+    return *value;
+  }
   if (auto window = MaskConfig::Profile().getWindow()) {
     if (auto value = window->getDevicePixelRatio()) {
       return *value;
@@ -3958,6 +3979,9 @@ int32_t nsGlobalWindowInner::GetScrollMaxY(ErrorResult& aError) {
 }
 
 double nsGlobalWindowInner::GetScrollX(ErrorResult& aError) {
+  if (auto value = MaskConfig::GetDouble("screen.pageXOffset")) {
+    return *value;
+  }
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getPageXOffset()) {
       return *value;
@@ -3967,6 +3991,9 @@ double nsGlobalWindowInner::GetScrollX(ErrorResult& aError) {
 }
 
 double nsGlobalWindowInner::GetScrollY(ErrorResult& aError) {
+  if (auto value = MaskConfig::GetDouble("screen.pageYOffset")) {
+    return *value;
+  }
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getPageYOffset()) {
       return *value;
diff --git a/layout/style/nsMediaFeatures.cpp b/layout/style/nsMediaFeatures.cpp
index 2e2bf927c0..922c70e970 100644
--- a/layout/style/nsMediaFeatures.cpp
+++ b/layout/style/nsMediaFeatures.cpp
@@ -84,6 +84,24 @@ static nsSize GetDeviceSize(const Document& aDocument) {
     return GetSize(aDocument);
   }
 
+  // Prefer launch-level Rotunda profiles over protocol viewport bookkeeping.
+  // Juggler uses ScreenDimensionManager for Playwright viewport emulation, but
+  // that must not replace the fingerprinted screen seen by page CSS.
+  auto configuredWidth = MaskConfig::GetInt32("screen.width");
+  auto configuredHeight = MaskConfig::GetInt32("screen.height");
+  if (configuredWidth && configuredHeight) {
+    return CSSPixel::ToAppUnits(
+        CSSIntSize(*configuredWidth, *configuredHeight));
+  }
+
+  if (auto screen = MaskConfig::Profile().getScreen()) {
+    auto width = screen->getWidth();
+    auto height = screen->getHeight();
+    if (width && height) {
+      return CSSPixel::ToAppUnits(CSSIntSize(*width, *height));
+    }
+  }
+
   // Rotunda: per-context screen dimensions for CSS device-width/height queries
   if (nsPIDOMWindowInner* inner = aDocument.GetInnerWindow()) {
     nsGlobalWindowInner* win = nsGlobalWindowInner::Cast(inner);
@@ -184,6 +202,10 @@ int32_t Gecko_MediaFeatures_GetMonochromeBitsPerPixel(
 }
 
 StyleColorGamut Gecko_MediaFeatures_ColorGamut(const Document* aDocument) {
+  if (auto value = MaskConfig::GetString("screen.colorGamut")) {
+    return RotundaColorGamutFromProfileValue(*value);
+  }
+
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getColorGamut()) {
       return RotundaColorGamutFromProfileValue(*value);
@@ -211,6 +233,25 @@ int32_t Gecko_MediaFeatures_GetColorDepth(const Document* aDocument) {
     return 0;
   }
 
+  if (auto colorDepth = MaskConfig::GetInt32("screen.colorDepth")) {
+    return *colorDepth / 3;
+  }
+  if (auto pixelDepth = MaskConfig::GetInt32("screen.pixelDepth")) {
+    return *pixelDepth / 3;
+  }
+
+  if (nsPIDOMWindowInner* inner = aDocument->GetInnerWindow()) {
+    nsGlobalWindowInner* win = nsGlobalWindowInner::Cast(inner);
+    if (win) {
+      uint32_t ucid =
+          mozilla::dom::ScreenDimensionManager::GetUserContextIdFromWindow(win);
+      int32_t value = mozilla::dom::ScreenDimensionManager::GetColorDepth(ucid);
+      if (value > 0) {
+        return value / 3;
+      }
+    }
+  }
+
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto colorDepth = screen->getColorDepth()) {
       return *colorDepth / 3;
@@ -408,6 +449,10 @@ StyleScripting Gecko_MediaFeatures_Scripting(const Document* aDocument) {
 }
 
 StyleDynamicRange Gecko_MediaFeatures_DynamicRange(const Document* aDocument) {
+  if (auto value = MaskConfig::GetString("screen.dynamicRange")) {
+    return RotundaDynamicRangeFromProfileValue(*value);
+  }
+
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getDynamicRange()) {
       return RotundaDynamicRangeFromProfileValue(*value);
@@ -422,6 +467,10 @@ StyleDynamicRange Gecko_MediaFeatures_DynamicRange(const Document* aDocument) {
 
 StyleDynamicRange Gecko_MediaFeatures_VideoDynamicRange(
     const Document* aDocument) {
+  if (auto value = MaskConfig::GetString("screen.videoDynamicRange")) {
+    return RotundaDynamicRangeFromProfileValue(*value);
+  }
+
   if (auto screen = MaskConfig::Profile().getScreen()) {
     if (auto value = screen->getVideoDynamicRange()) {
       return RotundaDynamicRangeFromProfileValue(*value);
